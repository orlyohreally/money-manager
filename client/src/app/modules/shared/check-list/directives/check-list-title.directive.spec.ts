import { CheckListTitleDirective } from './check-list-title.directive';

describe('CheckListTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckListTitleDirective();
    expect(directive).toBeTruthy();
  });
});
