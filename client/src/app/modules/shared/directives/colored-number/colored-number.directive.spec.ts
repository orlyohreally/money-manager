import { ColoredNumberDirective } from './colored-number.directive';

describe('ColoredNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoredNumberDirective();
    expect(directive).toBeTruthy();
  });
});
