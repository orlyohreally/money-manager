import { NotificationBlockDirective } from './notification-block.directive';

describe('NotificationBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new NotificationBlockDirective();
    expect(directive).toBeTruthy();
  });
});
